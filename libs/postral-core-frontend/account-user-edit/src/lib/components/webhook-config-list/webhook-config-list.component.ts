import { Component, signal } from '@angular/core';
import { ActivatedRoute, Router } from '@angular/router';

@Component({
  selector: 'lib-webhook-config-list',
  standalone: false,
  templateUrl: './webhook-config-list.component.html',
  styleUrl: './webhook-config-list.component.scss',
})
export class WebhookConfigListComponent {}
