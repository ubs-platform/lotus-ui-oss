# language-management

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build language-management` to build the library.

## Running unit tests

Run `nx test language-management` to execute the unit tests via [Jest](https://jestjs.io).
