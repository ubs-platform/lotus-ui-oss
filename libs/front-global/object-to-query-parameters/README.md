# object-to-query-parameters

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build object-to-query-parameters` to build the library.

## Running unit tests

Run `nx test object-to-query-parameters` to execute the unit tests via [Jest](https://jestjs.io).
