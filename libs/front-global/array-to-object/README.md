# array-to-object

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build array-to-object` to build the library.

## Running unit tests

Run `nx test array-to-object` to execute the unit tests via [Jest](https://jestjs.io).
