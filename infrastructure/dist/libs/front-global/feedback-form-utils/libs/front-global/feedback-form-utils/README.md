# front-global-feedback-form-utils

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build front-global-feedback-form-utils` to build the library.

## Running unit tests

Run `nx test front-global-feedback-form-utils` to execute the unit tests via [Jest](https://jestjs.io).
