# lotus-common/books

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build lotus-common/books` to build the library.

## Running unit tests

Run `nx test lotus-common/books` to execute the unit tests via [Jest](https://jestjs.io).
